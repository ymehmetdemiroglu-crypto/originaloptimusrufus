Show full details for a prospect — score, platform, pain summary, generated DM, notes, follow-up count.

Usage: /show <prospect_id>

```
cd "C:\Users\hp\OneDrive\Desktop\first client\acquisition_tool" && python main.py show $ARGUMENTS
```
