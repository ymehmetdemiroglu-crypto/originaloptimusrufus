Add a timestamped note to a prospect — useful for tracking replies, context, or next steps.

Usage: /note <prospect_id> <your note text>

Example: /note abc12345 they asked about pricing, interested but wants a demo first

```
cd "C:\Users\hp\OneDrive\Desktop\first client\acquisition_tool" && python main.py note $ARGUMENTS
```
